'use client';
import React from 'react';
import GenericStylesShowcase from '@/components/shared/GenericStylesShowcase';

const StylesShowcase = () => {
    // Usar el componente genérico con el ID de categoría específico para chicos (7)
    return <GenericStylesShowcase categoryId={7} />;
};

export default StylesShowcase;
