'use client';
import React from 'react';
import GenericStylesShowcase from '@/components/shared/GenericStylesShowcase';

const StylesShowcase = () => {
    // Usar el componente genérico con el ID de categoría específico para hombres (1)
    return <GenericStylesShowcase categoryId={1} />;
};

export default StylesShowcase;
