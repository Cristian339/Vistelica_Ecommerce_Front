'use client';
import React from 'react';
import GenericStylesShowcase from '@/components/shared/GenericStylesShowcase';

const StylesShowcase = () => {
    // Usar el componente genérico con el ID de categoría específico para chicas (2)
    return <GenericStylesShowcase categoryId={2} />;
};

export default StylesShowcase;
